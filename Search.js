import SearchField from "react-search-field";

const Search = () => {
  return (
    <SearchField
      className="Search"
      placeholder="Search..."
      // onChange={onChange}
      // searchText="This is initial search text for Searching Book"
      classNames="test-class"
    />
  );
};
export default Search;
